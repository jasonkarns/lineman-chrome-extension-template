{
  "manifest_version": 2,
  "name": "<%= pkg.name %>",
  "version": "<%= pkg.version %>",
  "description": "<%= pkg.description %>",

  "background": {
    "scripts": [
      "<%= files.background.js.concatenated %>"
    ]
  }
}
